services:
  miningbots-server:
    image: miningbots-frontend-server
    container_name: miningbots-frontend-server
    ports:
      - "8000:8000"
    networks:
      - miningbots-frontend

networks:
  miningbots-frontend:
    driver: bridge
